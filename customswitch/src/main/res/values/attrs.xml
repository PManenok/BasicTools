<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="CustomSwitch">
        <!--Title attrs-->
        <attr name="switchTitle" format="string" />
        <attr name="switchTitleTextAppearance" format="reference" />
        <attr name="switchTitlePaddingStart" format="dimension" />
        <attr name="switchTitlePaddingTop" format="dimension" />
        <attr name="switchTitlePaddingEnd" format="dimension" />
        <attr name="switchTitlePaddingBottom" format="dimension" />
        <!--Value attrs-->
        <attr name="switchInfo" format="string" />
        <attr name="switchInfoTextAppearance" format="reference" />
        <attr name="switchInfoPaddingStart" format="dimension" />
        <attr name="switchInfoPaddingTop" format="dimension" />
        <attr name="switchInfoPaddingEnd" format="dimension" />
        <attr name="switchInfoPaddingBottom" format="dimension" />
        <attr name="switchInfoAlignment" format="integer">
            <!-- Default. -->
            <enum name="inherit" value="0" />
            <!-- Default for the root view. The gravity determines the alignment, ALIGN_NORMAL,
                ALIGN_CENTER, or ALIGN_OPPOSITE, which are relative to each paragraph’s
                text direction. -->
            <enum name="gravity" value="1" />
            <!-- Align to the start of the paragraph, for example: ALIGN_NORMAL. -->
            <enum name="textStart" value="2" />
            <!-- Align to the end of the paragraph, for example: ALIGN_OPPOSITE. -->
            <enum name="textEnd" value="3" />
            <!-- Center the paragraph, for example: ALIGN_CENTER. -->
            <enum name="center" value="4" />
            <!-- Align to the start of the view, which is ALIGN_LEFT if the view’s resolved
                layoutDirection is LTR, and ALIGN_RIGHT otherwise. -->
            <enum name="viewStart" value="5" />
            <!-- Align to the end of the view, which is ALIGN_RIGHT if the view’s resolved
                layoutDirection is LTR, and ALIGN_LEFT otherwise. -->
            <enum name="viewEnd" value="6" />
        </attr>
        <!--Switcher attrs-->
        <attr name="thumbTint" format="color" />
        <attr name="trackTint" format="color" />
    </declare-styleable>
</resources>